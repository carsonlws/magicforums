$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

<% if @comment.persisted? %>
  $('#comment-update-form').replaceWith("<%=j render partial: 'comments', locals: { comment: @comment, post: @comment.post, topic: @topic } %>")
<% end %>
